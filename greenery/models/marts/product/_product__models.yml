version: 2

models:

  - name: fct_product_pageview_daily
    description: Aggregated fact model of pageviews for individual products. Each row corresponds to one day per product.
    columns:
      - name: pageview_date
      - name: product_id
      - name: product_name
      - name: pageview_count
      - name: user_count